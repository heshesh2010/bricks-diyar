name: ios_swiftui_module
version: 0.1.0
description: A SwiftUI MVVM module template for iOS projects.
vars:
  name:
    type: string
    description: The feature name
    default: example
    prompt: What is the feature name?
